const buttons = document.querySelectorAll('button')

console.log(buttons);

const qtdButtons = document.querySelectorAll('.qtd button');

console.log(qtdButtons, 'qtbButtons');


const byButtons = document.querySelectorAll('.card > button');

console.log(byButtons, 'byButtons');